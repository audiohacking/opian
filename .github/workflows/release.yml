name: Build Release

on:
  release:
    types: [published]

jobs:
  build-macOS:
    name: Build macOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache JUCE FetchContent
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: ${{ runner.os }}-juce-8.0.8-${{ hashFiles('CMakeLists.txt') }}
          restore-keys: |
            ${{ runner.os }}-juce-8.0.8-

      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DOPIAN_COPY_PLUGIN_AFTER_BUILD=OFF

      - name: Build plugins, standalone, and tests
        run: cmake --build build -j8 --config Release --target OPIAN_Standalone OPIAN_VST3 OPIAN_AU opian_tests

      - name: Test
        run: ctest --test-dir build --output-on-failure -C Release

      - name: Verify artefacts
        run: |
          test -d build/OPIAN_artefacts/Release/AU/OPIAN.component
          test -d build/OPIAN_artefacts/Release/VST3/OPIAN.vst3
          test -d build/OPIAN_artefacts/Release/Standalone/OPIAN.app

      - name: Package (zip)
        run: |
          cd build/OPIAN_artefacts/Release
          zip -r ../../../OPIAN-macOS.zip AU/ VST3/ Standalone/
          cd Standalone
          zip -r ../../../../OPIAN-macOS-Standalone.zip OPIAN.app

      - name: Package (installer .pkg)
        run: |
          chmod +x scripts/build-installer-pkg.sh
          ./scripts/build-installer-pkg.sh --version ${{ github.event.release.tag_name }}

      - name: Upload zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: OPIAN-macOS
          path: OPIAN-macOS.zip

      - name: Upload standalone artifact
        uses: actions/upload-artifact@v4
        with:
          name: OPIAN-macOS-Standalone
          path: OPIAN-macOS-Standalone.zip

      - name: Upload pkg artifact
        uses: actions/upload-artifact@v4
        with:
          name: OPIAN-macOS-Installer
          path: release-artefacts/OPIAN-macOS-Installer.pkg

  build-windows:
    name: Build Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache JUCE FetchContent
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: ${{ runner.os }}-juce-8.0.8-${{ hashFiles('CMakeLists.txt') }}
          restore-keys: |
            ${{ runner.os }}-juce-8.0.8-

      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DOPIAN_COPY_PLUGIN_AFTER_BUILD=OFF

      - name: Build plugins, standalone, and tests
        run: cmake --build build -j8 --config Release --target OPIAN_Standalone OPIAN_VST3 opian_tests

      - name: Test
        run: ctest --test-dir build --output-on-failure -C Release

      - name: Verify artefacts
        shell: bash
        run: |
          test -d build/OPIAN_artefacts/Release/VST3/OPIAN.vst3
          test -f build/OPIAN_artefacts/Release/Standalone/OPIAN.exe

      - name: Package (zip)
        shell: bash
        run: |
          cd build/OPIAN_artefacts/Release
          7z a -tzip ../../../OPIAN-Windows.zip VST3/ Standalone/
          cd Standalone
          7z a -tzip ../../../../OPIAN-Windows-Standalone.zip OPIAN.exe

      - name: Upload zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: OPIAN-Windows
          path: OPIAN-Windows.zip

      - name: Upload standalone artifact
        uses: actions/upload-artifact@v4
        with:
          name: OPIAN-Windows-Standalone
          path: OPIAN-Windows-Standalone.zip

  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libasound2-dev \
            libjack-jackd2-dev \
            ladspa-sdk \
            libcurl4-openssl-dev \
            libfreetype-dev \
            libfontconfig1-dev \
            libx11-dev \
            libxcomposite-dev \
            libxcursor-dev \
            libxext-dev \
            libxinerama-dev \
            libxrandr-dev \
            libxrender-dev \
            libxi-dev \
            libwebkit2gtk-4.1-dev \
            libglu1-mesa-dev \
            mesa-common-dev \
            libegl-dev

      - name: Cache JUCE FetchContent
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: ${{ runner.os }}-juce-8.0.8-${{ hashFiles('CMakeLists.txt') }}
          restore-keys: |
            ${{ runner.os }}-juce-8.0.8-

      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DOPIAN_COPY_PLUGIN_AFTER_BUILD=OFF

      - name: Build plugins, standalone, and tests
        run: cmake --build build -j8 --config Release --target OPIAN_Standalone OPIAN_VST3 opian_tests

      - name: Test
        run: ctest --test-dir build --output-on-failure -C Release

      - name: Verify artefacts
        run: |
          test -d build/OPIAN_artefacts/Release/VST3/OPIAN.vst3
          test -x build/OPIAN_artefacts/Release/Standalone/OPIAN

      - name: Package (zip)
        run: |
          cd build/OPIAN_artefacts/Release
          zip -r ../../../OPIAN-Linux.zip VST3/ Standalone/
          cd Standalone
          zip -r ../../../../OPIAN-Linux-Standalone.zip OPIAN

      - name: Upload zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: OPIAN-Linux
          path: OPIAN-Linux.zip

      - name: Upload standalone artifact
        uses: actions/upload-artifact@v4
        with:
          name: OPIAN-Linux-Standalone
          path: OPIAN-Linux-Standalone.zip

  create-release:
    name: Create Release Assets
    needs: [build-macOS, build-windows, build-linux]
    if: always() && needs.build-macOS.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download macOS zip
        uses: actions/download-artifact@v4
        with:
          name: OPIAN-macOS
          path: ./artifacts

      - name: Download macOS standalone
        uses: actions/download-artifact@v4
        with:
          name: OPIAN-macOS-Standalone
          path: ./artifacts

      - name: Download macOS installer
        uses: actions/download-artifact@v4
        with:
          name: OPIAN-macOS-Installer
          path: ./artifacts

      - name: Download Windows zip
        if: needs.build-windows.result == 'success'
        uses: actions/download-artifact@v4
        with:
          name: OPIAN-Windows
          path: ./artifacts

      - name: Download Windows standalone
        if: needs.build-windows.result == 'success'
        uses: actions/download-artifact@v4
        with:
          name: OPIAN-Windows-Standalone
          path: ./artifacts

      - name: Download Linux zip
        if: needs.build-linux.result == 'success'
        uses: actions/download-artifact@v4
        with:
          name: OPIAN-Linux
          path: ./artifacts

      - name: Download Linux standalone
        if: needs.build-linux.result == 'success'
        uses: actions/download-artifact@v4
        with:
          name: OPIAN-Linux-Standalone
          path: ./artifacts

      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
